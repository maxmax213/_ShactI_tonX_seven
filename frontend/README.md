# Frontend

React + TypeScript + Vite client for Edu Platform.

## Run

```bash
npm install
npm run dev
```

By default frontend expects API at `http://localhost:8000/api`.
Configure via `.env`:

```env
VITE_API_URL=http://localhost:8000/api
```
